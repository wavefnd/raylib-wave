extern(c) {
    fun LoadImage(fileName: ptr<u8>) -> Image;
    fun UnloadImage(image: Image);

    fun LoadTexture(fileName: ptr<u8>) -> Texture2D;
    fun UnloadTexture(texture: Texture2D);

    fun DrawTexture(
        texture: Texture2D,
        posX: i32,
        posY: i32,
        tint: Color
    );

    fun DrawTextureRec(
        texture: Texture2D,
        source: Rectangle,
        position: Vector2,
        tint: Color
    );
}