extern(c) {
    fun InitWindow(width: i32, height: i32, title: ptr<u8>);
    fun CloseWindow();

    fun WindowShouldClose() -> i32;

    fun BeginDrawing();
    fun EndDrawing();

    fun ClearBackground(color: Color);

    fun SetTargetFPS(fps: i32);
    fun GetFPS() -> i32;

    fun GetScreenWidth() -> i32;
    fun GetScreenHeight() -> i32;
}